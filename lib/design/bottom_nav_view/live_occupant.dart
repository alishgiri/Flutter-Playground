import 'package:flutter/material.dart';

class LiveOccupants extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: <Widget>[
          Text('Live Occupants'),
        ],
      ),
    );
  }
}
