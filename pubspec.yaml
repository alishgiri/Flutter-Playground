name: test_in_flutter
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^0.19.0
  cupertino_icons: ^0.1.3
  font_awesome_flutter: ^8.11.0
  nfc_in_flutter: ^2.0.5
  qr_code_scanner: ^0.3.0
  qr_flutter: ^3.2.0
  udp: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/app_icon.png
